import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.greenAccent;
  static const background = Colors.black;
  static const text = Colors.white;
}

class AppText {
  static const appName = "Vertex";
}
